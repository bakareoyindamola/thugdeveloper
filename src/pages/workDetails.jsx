// import { useParams } from "react-router-dom";
import { WorkDetailsContainer} from "../containers";

export default function WorkDetails() {
    // let { name } = useParams();
    return <WorkDetailsContainer />
}
